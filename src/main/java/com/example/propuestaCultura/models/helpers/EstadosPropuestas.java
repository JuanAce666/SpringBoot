package com.example.propuestaCultura.models.helpers;

import jakarta.persistence.Entity;
import jakarta.persistence.Table;

public enum EstadosPropuestas {
    INICIO,
    APROBADO,
    RECHAZADO,
    FINALIZADO
}
